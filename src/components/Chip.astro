---
// import type { Chip } from "@src/types.js";

export interface Props {
  tag: string;
  class?: string;
}

const { tag, class: className } = Astro.props as Props;
---

<div
  class:list={[
    "flex items-center justify-center text-sm text-text-gray-900  shadow-2xl border  max-w-fit px-2 py-1 sm:p-2 ",
    className,
  ]}
>
  {tag}
</div>
