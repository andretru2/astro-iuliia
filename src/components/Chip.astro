---
// import type { Chip } from "@src/types.js";

export interface Props {
  tag: string;
  class?: string;
}

const { tag, class: className } = Astro.props as Props;
---

<div
  class:list={[
    "flex items-center justify-center text-md text-text-slate-700 font-normal bg-inherit/80 rounded-md shadow-xl border-secondary border max-w-fit px-3 py-2",
    className,
  ]}
>
  {tag}
</div>
